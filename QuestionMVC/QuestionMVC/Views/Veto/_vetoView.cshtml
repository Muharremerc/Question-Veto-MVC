@model List<DAL.Model.Veto>
<div class="form-group">

    <div class="col-sm-10">
        <table>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                </tr>
            }

        </table>
    </div>
</div>
